export const devInfo = function() {
    return `<p>ENV: ${process.env.NODE_ENV}</p>`;
};

export const intro = () => {
    return `<blockquote>
            You must be the change you wish to see in the world.
            -- Mahatma Gandhi
            </blockquote>`;
};
